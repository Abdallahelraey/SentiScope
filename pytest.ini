[pytest]
testpaths = tests
pythonpath = src
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = -v -rx -l --tb=short --strict-markers
markers =
    integration: marks tests as integration tests
    unit: marks tests as unit tests
